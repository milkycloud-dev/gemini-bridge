name: gemini_bridge_client
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.3

  flutter_markdown: ^0.7.7+1
  file_picker: ^11.0.2
  path_provider: ^2.1.6
  dio: ^5.10.0
  dartssh2: ^2.22.0
  device_info_plus: ^11.0.0
  flutter_spinkit: ^5.2.0
  http_parser: ^4.0.2
  mime: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
